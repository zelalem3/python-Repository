from tkinter import *

window = Tk()
window.title("mile to kilometer convertor")
window.minsize(width= 500,height= 500)

my_label = Label(text="enter the value")
another_label = Label(text="miles")
another_label.grid(column=2, row=0)
entry = Entry()
button = entry(text="enter value")
button.grid(column=1,row=0)

e
window.mainloop()